image_size: 64
data_depth: 3
hidden_size: 64
num_heads: 2
num_layers: 2
mlp_ratio: 2.0
window_size: 4
patch_size: 4
dropout: 0.1

learning_rate_encoder: 0.0003
learning_rate_decoder: 0.0003
learning_rate_discriminator: 0.0002
beta1: 0.5
beta2: 0.999

batch_size: 4
num_epochs: 30
num_workers: 0

lambda_image: 5.0
lambda_adv: 0.01
lambda_bit: 2.0

robustness:
  enable: false

data_path: ./data/datasets
checkpoint_path: ./checkpoints
log_dir: ./runs
seed: 42
